import { useCallback, useEffect, useState } from "react";
import { RiFileCopyLine as Copy, RiKey2Line as KeyRound, RiLoader4Line as LoaderCircle, RiAddLine as Plus, RiDeleteBinLine as Trash2 } from "@remixicon/react";
import { Alert, AlertDescription } from "@/components/ui/alert";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardHeader, CardTitle } from "@/components/ui/card";
import { Separator } from "@/components/ui/separator";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table";
import { Tooltip, TooltipContent, TooltipTrigger } from "@/components/ui/tooltip";
import { apiKeys } from "../api/client";
import ConfirmDialog from "../components/ConfirmDialog";
import type { ApiKey, ApiKeyWithSecret } from "../types";

export default function ApiKeysPage() {
  const [keys, setKeys] = useState<ApiKey[]>([]); const [loading, setLoading] = useState(true); const [createOpen, setCreateOpen] = useState(false); const [secret, setSecret] = useState<ApiKeyWithSecret | null>(null); const [name, setName] = useState(""); const [saving, setSaving] = useState(false); const [error, setError] = useState<string | null>(null); const [removeTarget, setRemoveTarget] = useState<ApiKey | null>(null);
  const load = useCallback(async () => { try { setLoading(true); setKeys(await apiKeys.list()); } catch (e: any) { setError(e.message); } finally { setLoading(false); } }, []); useEffect(() => { load(); }, [load]);
  const create = async () => { if (!name.trim()) return; setSaving(true); try { setSecret(await apiKeys.create(name.trim())); setName(""); setCreateOpen(false); await load(); } catch (e: any) { setError(e.message); } finally { setSaving(false); } };
  const remove = async () => { if (!removeTarget) return; try { await apiKeys.remove(removeTarget.id); setKeys((items) => items.filter((item) => item.id !== removeTarget.id)); setRemoveTarget(null); } catch (e: any) { setError(e.message); } };
  const copy = (value: string) => navigator.clipboard?.writeText(value);
  if (loading) return <div className="flex justify-center p-12"><LoaderCircle className="size-5 animate-spin text-muted-foreground" /></div>;
  return <div className="w-full space-y-6 p-6"><div className="flex items-center justify-between gap-4"><h1 className="font-heading text-2xl font-semibold tracking-tight">API keys</h1><Button onClick={() => setCreateOpen(true)}><Plus className="size-4" />Generate key</Button></div>{error && <Alert variant="destructive"><AlertDescription>{error}</AlertDescription></Alert>}{keys.length === 0 ? <div className="rounded-xl border border-dashed p-12 text-center"><KeyRound className="mx-auto size-8 text-muted-foreground" /><h2 className="mt-3 font-heading text-lg font-medium">No API keys</h2><p className="mt-1 text-sm text-muted-foreground">Generate a key to use the Klove proxy.</p><Button className="mt-5" onClick={() => setCreateOpen(true)}>Generate your first key</Button></div> : <Card className="overflow-hidden p-0 gap-0"><CardHeader className="flex flex-row items-center justify-between py-(--card-spacing)"><CardTitle>Active keys ({keys.length})</CardTitle></CardHeader><Separator /><Table><TableHeader><TableRow><TableHead>Name</TableHead><TableHead>Prefix</TableHead><TableHead>Status</TableHead><TableHead>Created</TableHead><TableHead className="w-20">Actions</TableHead></TableRow></TableHeader><TableBody>{keys.map((key) => <TableRow key={key.id}><TableCell className="font-medium">{key.name}</TableCell><TableCell className="font-mono text-xs text-muted-foreground">{key.prefix}</TableCell><TableCell><Badge variant={key.is_active ? "secondary" : "outline"}>{key.is_active ? "Active" : "Inactive"}</Badge></TableCell><TableCell className="text-sm text-muted-foreground">{new Date(key.created_at).toLocaleDateString()}</TableCell><TableCell><Tooltip><TooltipTrigger render={<Button size="icon" variant="ghost" className="text-destructive" onClick={() => setRemoveTarget(key)} />}><Trash2 className="size-4" /></TooltipTrigger><TooltipContent>Revoke key</TooltipContent></Tooltip></TableCell></TableRow>)}</TableBody></Table></Card>}<Dialog open={createOpen} onOpenChange={setCreateOpen}><DialogContent><DialogHeader><DialogTitle>Generate API key</DialogTitle><DialogDescription>Create a new key for programmatic access.</DialogDescription></DialogHeader><div className="space-y-4"><div className="space-y-2"><Label htmlFor="key-name">Key name</Label><Input id="key-name" value={name} onChange={(e) => setName(e.target.value)} placeholder="e.g. Production" /></div></div><DialogFooter><Button variant="outline" onClick={() => setCreateOpen(false)}>Cancel</Button><Button onClick={create} disabled={saving || !name.trim()}>{saving ? "Generating..." : "Generate"}</Button></DialogFooter></DialogContent></Dialog>{secret && <Dialog open onOpenChange={(open) => { if (!open) setSecret(null); }}><DialogContent><DialogHeader><DialogTitle>API key created</DialogTitle><DialogDescription>Copy this key now. You will not be able to see it again.</DialogDescription></DialogHeader><div className="space-y-2"><Label>Secret key</Label><div className="flex gap-2"><Input readOnly value={secret.secret} className="font-mono text-xs" /><Tooltip><TooltipTrigger render={<Button size="icon" variant="outline" onClick={() => copy(secret.secret)} />}><Copy className="size-4" /></TooltipTrigger><TooltipContent>Copy to clipboard</TooltipContent></Tooltip></div></div><DialogFooter><Button onClick={() => { copy(secret.secret); setSecret(null); }}>Copied</Button></DialogFooter></DialogContent></Dialog>}<ConfirmDialog open={!!removeTarget} title="Revoke API key" message={`Revoke key "${removeTarget?.name}"? This cannot be undone.`} confirmLabel="Revoke" onConfirm={remove} onCancel={() => setRemoveTarget(null)} /></div>;
}
